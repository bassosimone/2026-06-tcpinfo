[project]
name = "2026-06-tcpinfo"
version = "0.1.0"
description = "Scripts to investigate TCP_INFO."
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["click", "pandas", "pyarrow", "statsmodels"]
